import _ from 'lodash';
import box from '../css/main.less';
// let flag = true;
// setInterval(() => {
// 	if (flag) {
// 		box.use();
// 	} else {
// 		box.unuse();
// 	}
// 	flag = !flag;
// }, 5000);

export default function createDiv (arr) {
	// let element = document.createElement('div');
	// element.innerHTML = _.join(arr, ' ');
	// element.classList.add('box');
	// return element;
};